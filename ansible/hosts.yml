hosts:
  localhost:
    vars:
      user: emiel
      user_home: /home/emiel
      ansible_connection: local
      ansible_python_interpreter: "{{ansible_playbook_python}}"
  devserver:
    vars:
      ansible_connection: local # define this later
